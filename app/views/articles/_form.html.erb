<%= form_for( @article) do |f| %>
  <% if @article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% @article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-2">
    <%= f.text_field  :title, label:"Nom du Projet", 
    class:"form-control focus-none p-2 border rounded-md",
     placeholder:"Titre de l'article..."  %>
  </div>
  
  <div class="my-8">
    <%= f.rich_text_area :content, hide_label:true,
     class:" bg-white border-2 rounded-md ",
      placeholder:"Contenu de l'article ..."  %>
  </div>
 
 <div class="my-8">
  <%= f.label 'Image de couverture' %>
  <div class="">
      <%= f.file_field :heroImg, class:"" %>
  </div>
 </div>
  <div class="group">
    <div class="my-2">
      <%= f.label "Date Publication:" %> <br>
      <%= f.date_field :write_date, class:" p-2 border rounded-md" , placeholder:"Date de publication" %>
    </div>
    
    <div class=" mb-4">
    
    <%= f.select :status, Article::STATUS, class:"p-6 border border-blue-500 rounded-md", paceholder:'Status...' %>
    </div>
  </div>
  </div>
 
      

  <div class="form-actions">
    <%= f.submit "Crée Article", class:"font-bold p-2 border rounded-md bg-green-500 font-semibold text-white my-2" %>
  </div>

<% end %>
