<div class="row row-cols-1 row-cols-md-2 g-4">
  <% @benevolsList.each do |bl| %>
  <div class="col">
    <div class="card">
     <%= image_tag(bl.img, class:"card-img-top h-40 bg-red-200") %> </h2>
      <div class="card-body">
      <div class="flex">
        <h5 class="card-title font-bold"><%= bl.full_name %></h5>
        
      </div>
        <p class="card-text"><%= bl.role %>.</p>
        <p class="card-text"><%= bl.lin_fb %>.</p>
        <p class="card-text"><%= bl.link_tw %>.</p>
        <p class="card-text"><%= bl.link_ldin %>.</p>
      </div>
    </div>
  </div>
  <% end %>
 
</div>